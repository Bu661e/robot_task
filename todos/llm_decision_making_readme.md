# llm_decision_making_readme 任务清单

- [x] 基于 `CodeGenSpec.md` 为 `llm_decision_making` 编写 README
- [x] 在 README 中明确 `robot_service` 与 `perception_service` 对本模块来说是黑盒
- [x] 在 README 中强调本模块只通过网络协议发送和接收数据
- [x] 在 README 中说明本模块内部各子模块的职责
- [x] 在 README 中将 `policy_executor` 改为远程调用 `robot_service` 执行策略代码
