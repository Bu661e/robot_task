# AGENTS

## 开发环境设置

- 当前模块部署目标：运行在云服务器上的 `robot_service`
- 当前目标后端：`isaac_sim`
- 当前目标版本：Isaac Sim `5.0.0`
- 当前建议语言：Python
- 当前默认工作环境就是云主机，按 Linux 主机上的远端服务来考虑开发、日志和排障。
- 当前云主机上是否已经具备可用的 Isaac Sim `5.0.0` 运行环境，需要以实际安装和启动结果为准；未验证前不要默认可用。
- 开发时先确认所用 Python 运行环境与 Isaac Sim `5.0.0` 兼容，不要默认假设系统 Python、独立 `.venv` 或其他解释器方案已经确定
- 包管理、虚拟环境和部署启动方式当前都还未最终定稿；如果后续确认，再单独更新本文档

## 文档

- 当前 `README.md` 可以更新，但只同步已经确认的模块结构、云主机运行约束和验证状态，不要补写未定方案。
- 早期部分 Plan 文档、Spec 文档或调研记录可能形成于本地开发阶段，因此会提到“本地环境”和“云环境”的区别；这些内容只作为历史背景参考，当前以及后续开发统一以云环境为准。
- 跨模块共享协议文档统一放在仓库根目录 `docs/protocols/`，当前与 `llm_decision_making` 的 HTTP 协议以 `docs/protocols/llm_decision_making__robot_service.md` 为准。
- 本模块内部设计、计划、部署说明等本地文档的目录位置当前也还未最终定稿，确认结构后再统一整理。

## 编码规范

### 文件组织

- 待定。
- 在和我确认目录结构之前，不要先入为主创建 `modules/`、`backends/`、`config/`、`utils/` 等目录，也不要把结构写死到 `README.md` 中。
- 如果当前任务必须落代码，先描述准备新增的文件和职责边界，等我确认后再实施。

### 命名约定

- Python 模块文件、函数、变量使用 `snake_case`，例如 `session_manager.py`、`create_session()`。
- 类、`dataclass`、Schema 类型使用 `PascalCase`，例如 `SessionRecord`、`TaskRecord`。
- 常量、环境变量名使用 `UPPER_SNAKE_CASE`。
- 配置文件名尽量与对应职责保持一致，采用 `snake_case`，例如 `task_manager_config.py`。
- HTTP 请求体、响应体中的字段名优先保持一致的 `snake_case` 风格，避免在模块间混用多套命名规则。

### 类型标注

- 新增或修改代码时，只要能够明确判断数据类型，就应显式写出类型标注，不要省略。
- 函数参数、返回值、类属性、`dataclass` 字段等，尽量补全类型信息。
- 函数入参类型应保持固定明确；如果参数是必需的，不要写成 `Type | None = None`、`Optional[Type] = None` 这类可空且带默认值的形式。
- 结构化数据不要传裸 `dict`。
- 在类型暂时无法确定时，先询问我，按理来说不会出现这种情况。

### 日志

- 使用 Python 标准库 `logging` 模块，配置日志同时输出到终端和文件。
- 当前模块运行在云服务器上，日志需要优先服务远端排障；每次启动服务或创建新的机器人 session 时，都要在固定目录 `runs/` 下创建对应子目录，推荐命名为 `runs/<timestamp>_<session_id>`，保存本次运行的全部日志和产物。
- 日志必须同时输出到终端和文件；当前任务目录中至少包含 `run.log`、`requests/`、`responses/`、`artifacts/`。
- 日志内容应覆盖 session 生命周期、task 状态流转、HTTP 请求与响应摘要、Isaac Sim 调用摘要，以及异常信息。
- 如果响应中包含图片、文件、点云、mask、深度图等内容，必须额外保存到 `artifacts/`，日志中只记录摘要和文件路径，不直接打印大块原始内容。

### 测试

- 测试统一使用 `pytest`。
- 优先覆盖 Schema 校验、session/task 状态流转、HTTP 路由 smoke test，以及 artifact 管理逻辑。
- 涉及 Isaac Sim `5.0.0` 的测试，要明确区分“纯 Python 单元测试”和“依赖仿真环境的联调测试”；默认优先 mock，不要把所有验证都绑定到真实 Isaac Sim 环境。
- 即使当前就在云主机/Linux 环境下，未完成实际启动或联调前，也不能声称完成真实启动验证、真实联调测试或 Isaac Sim 内部行为验证。
- 如果确实需要跑 Isaac Sim `5.0.0` 联调或 smoke test，先说明将使用的云主机环境、启动方式和验证目标，再执行。
- 每次修改后，至少运行与本次改动直接相关的测试。

### 审查

- 采用轻量自审，不要求正式多人 code review 流程。
- 自审重点包括：是否破坏模块边界、是否偏离共享协议、是否把 Isaac Sim `5.0.0` 细节错误泄漏到协议层、类型和 Schema 是否清楚、日志是否补全、协议文档是否同步。
- 关键改动完成后，如有需要，可再补一次 Codex review。

### Git 提交

- 提交的 Message 用中文。
- 在本地仓库提交后直接推送到远程。

## 开发时注意事项

- 当前模块是运行在云服务器上的远端服务，不是本地一次性脚本；设计、日志、启动方式和异常处理都要按长期运行服务考虑。
- 当前阶段先按 `isaac_sim` + Isaac Sim `5.0.0` 约束思考实现；如果要扩展到 `real_robot` 或其他 backend，先询问我，不要自行扩展目标范围。
- 当前可以在云主机上做代码编写、文档整理以及不依赖真实 Isaac Sim 的验证；凡是依赖 GPU、Isaac Sim `5.0.0` 或特定云端部署配置的结论，只要没有实测就必须明确标注为“尚未实测”。
- 在编写涉及 Isaac Sim API、扩展包、启动参数、headless 模式或 GPU 依赖的代码前，先确认这些内容是否已经定稿；没定稿就先问我，不要自行假设。
- 修改 `README.md` 时，只同步已经确认的事实和当前验证状态，不要为了“保持同步”补写未定内容。
- 在编写接口协议代码前，先检查仓库根目录 `docs/protocols/llm_decision_making__robot_service.md` 与当前任务是否一致，如果不一致询问是否更改对应协议文档或更改任务。
- 对于每个模块输入和输出，应该设计一个 Schema；如果文档中没有设计，请先设计好让我检查，之后再写入文档，并在代码中实现这个 Schema。
- 当开始一个新模块编写时，使用 `writing-plans` skill，写出开发计划。

## 额外要求

- 当你有任何不清楚的地方，或是我没有表达清楚的地方，请务必先问我，而不是自己猜测着写代码。
- 当前 README 中内容还不完整，或是我对于其中具体内容还是模糊的。等实现完某个模块或数据结构时候，需要回过头来完善 README 中对应的内容。
- 当你觉得该 git 提交了就提醒我。
- 禁止使用 subagent，如果一定要使用在使用前询问我，使用的模型是 gpt-5.4-medium。
